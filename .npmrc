# ignore-scripts not set globally — example/package.json uses postinstall=patch-package
# for required dependency patches, and react-native may use native postinstall scripts.
# Apply --ignore-scripts selectively per CI workflow step instead (see .github/workflows/).
